# SPA serving: unknown paths (e.g. /:id spec routes) fall back to
# index.html so client-side routing works on direct loads.
:80

root * /usr/share/caddy
encode gzip

@static path /assets/*
header @static Cache-Control "public, max-age=31536000, immutable"

try_files {path} /index.html
file_server
